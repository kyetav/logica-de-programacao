const usuario = {
  email: 'victor@cod3r.com.br',
};

usuario.nome = 'Victor';
usuario.senha = '@!jahsj';

console.log(usuario.email);
console.log(usuario.nome);
console.log(usuario.senha);
console.log(usuario);
