const numeros = [1, 2, 3, 4, 5, 4];

numeros.splice(1, 2); // passa o indice, e a quantidade que tu quer apagar dps dele
console.log(numeros);

numeros.pop(); // apaga o ultimo elemto do array
console.log(numeros.pop());
console.log(numeros.pop());
console.log(numeros);
